"""RAG package for the DukeSchedulingAssistant backend.

This is part of the `backend` Python package, so modules should be
imported as `backend.rag.*` rather than using relative imports when
run via `python -m rag.ingest` from the backend directory.
"""
